package Abstraction;

public interface Transport {
    void getsetgo();             //after declaring an interface class and becoming the parent class of Gadi now the method needs
                                 // to be initialized by the child class Bently (must) and abstract class Gadi can or cannot initialize
}
